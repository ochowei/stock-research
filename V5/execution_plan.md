### **📈 專案 V5-ML 執行計畫：跨截面均值回歸與黑天鵝防禦系統**

**Date:** 2025-11-25

**Based on:** V5/research.md

**Status:** \#draft \#execution-plan

此計畫旨在實作 V5-ML 體系，將傳統的均值回歸策略（L2）與機器學習市場體制識別（L1）及元標籤過濾（L3）相結合。

#### **步驟 0：環境準備與數據擴展 (Setup & Data Expansion)**

V5 需要比 V4 更廣泛的市場數據來構建「體制識別層 (L1)」。除了個股數據外，還需要大盤指數與波動率數據。

* **目標：** 建立包含個股 (Universe) 與宏觀市場指標 (Macro) 的數據庫。  
* **執行：**  
  1. **資產池更新：** 鎖定高流動性標的 (S\&P 100 / Nasdaq 100)。  
  2. **市場數據獲取：** 新增下載腳本，獲取 SPY (大盤)、QQQ (科技股)、IWO (Russell 2000 Growth)、VTI (Total Market)、^VIX (恐慌指數)、^TNX (10年期公債殖利率) 的歷史數據。
  3. **個股數據獲取：** 下載成分股的 OHLCV 數據 (建議使用 Daily 頻率為主，60m 為輔)。  
* **產出檔案：**  
  * data/universe\_daily.parquet: 成分股面板數據。  
  * data/market\_indicators.parquet: VIX, SPY, 債券收益率等宏觀數據。

#### **步驟 1：特徵工程 \- L0 數據層 (L0 Feature Engineering)**

此步驟將原始價格轉換為模型可用的特徵，分為「市場脆弱性特徵」與「個股訊號特徵」。

* **目標：** 構建供 L1 (HMM/IsoForest) 與 L2 (Base Strategy) 使用的特徵集。  
* **執行：**  
  1. **市場特徵 (For L1):**  
     * 計算 **Proxy ETF 特徵:**
       * **IWO_Volatility:** IWO 的滾動已實現波動率。
       * **SPY_IWO_Divergence:** SPY 與 IWO 之間的滾動回報差異。
     * 計算 **波動率特徵:** VIX 的變動率、SPY 的 Realized Volatility。  
  2. **個股特徵 (For L2 & L3):**  
     * **均值回歸因子:** RSI(2), Bollinger Band %B, Distance to SMA200。  
     * **相對強弱:** 相對於 SPY 的 Beta 與 Alpha。  
* **產出檔案：**  
  * features/market\_features\_L0.parquet: 用於體制識別的時間序列數據。  
  * features/stock\_features\_L0.parquet: 用於個股選擇的面板數據。

#### **步驟 2：L1 體制識別模型訓練 (Regime Identification Layer)**

實作「黑天鵝防禦系統」，訓練非監督式學習模型來識別市場狀態。

* **目標：** 產出市場狀態訊號 (State) 與異常分數 (Anomaly Score)。  
* **執行：**  
  1. **HMM 模型訓練:**  
     * 使用 market\_features\_L0 (含 Proxy ETF 特徵) 訓練 Gaussian HMM。
     * 設定 3 種隱藏狀態：牛市 (Bull)、震盪 (Chop)、崩盤 (Crash)。  
     * 校準：確認 "Crash" 狀態能對應到歷史空頭年份 (2020, 2022)。  
  2. **Isolation Forest 訓練:**  
     * 訓練異常檢測模型，輸出異常分數。  
     * 設定閾值，定義何為「未知風險」。  
* **產出檔案：**  
  * models/hmm\_model.joblib: 訓練好的 HMM 模型。  
  * models/iso\_forest.joblib: 訓練好的 Isolation Forest 模型。  
  * signals/regime\_signals.parquet: 每日市場狀態與異常分數的時間序列。

#### **步驟 3：L2 策略生成與 L3 元標籤構建 (Strategy & Meta-Labeling)**

這是核心邏輯的整合步驟。先生成基礎訊號，再標記這些訊號的真實結果，最後訓練過濾模型。

* **目標：** 建立 V5 基礎策略並訓練 L3 過濾器。  
* **執行：**  
  1. **L2 基礎策略模擬 (Offense):**  
     * 邏輯：當 Price \> SMA200 且 RSI(2) \< 10 時產生訊號。  
     * 排序：每日選取 RSI 最低的 Top K 檔股票。  
     * 出場：持有 5 天或 RSI \> 70 (可選)。  
     * **生成初步訊號列表**。  
  2. **標籤生成 (Labeling):**  
     * 計算每個 L2 訊號在未來 5 天的損益。  
     * 定義 **Meta-Label (Y)**: 若獲利 \> 0 (或 Sharpe \> X)，則 Y=1，否則 Y=0。  
  3. **L3 模型訓練 (Optimization):**  
     * 輸入：stock\_features\_L0 \+ regime\_signals (將市場狀態作為上下文特徵)。  
     * 模型：XGBoost 或 Random Forest Classifier。  
     * 目標：預測 $P(Profit \> 0 | Signal, Context)$。  
* **產出檔案：**  
  * signals/base\_strategy\_trades.parquet: 未經過濾的原始交易紀錄。  
  * models/l3\_meta\_filter.joblib: L3 過濾模型。  
  * signals/l3\_probabilities.csv: 每個原始訊號的獲利機率預測。

#### **步驟 4：回測與驗證協議 (Backtest & Verification)**

執行 research.md 第 5 節定義的嚴格驗證測試。

* **目標：** 比較 V5-Base, V5+L1, V5+L3 的績效差異。  
* **執行：**  
  1. **基準測試 (Baseline):** 計算 V5 基礎策略的 Sharpe, Drawdown。  
  2. **測試 A (防禦滯後性):** 比較 HMM 識別出 "Crash" 狀態的時間點與市場高點的差距 (Target \< 5 days)。  
  3. **測試 B (毒性過濾):** 評估 L3 模型是否成功過濾掉虧損 \> 5% 的交易 (Toxic Signals)，同時保持足夠的召回率 (Recall)。  
  4. **綜合回測:** 模擬最終資金曲線 (Equity Curve)。  
     * 策略邏輯：L2 生成訊號 \-\> L1 檢查市場狀態 (若 Crash 則熔斷) \-\> L3 檢查獲利機率 (若 Prob \< Threshold 則過濾) \-\> 執行交易。  
* **產出檔案：**  
  * analysis/backtest\_report.txt: 包含 Sharpe, DD, Win Rate 的詳細報告。  
  * analysis/equity\_curves\_comparison.png: 策略績效比較圖。  
  * analysis/regime\_overlay.png: 市場走勢疊加 HMM 狀態圖。

#### **步驟 5：合成壓力測試 (Optional/Advanced)**

* **目標：** 使用 TimeGAN 生成未見過的市場路徑，測試 L1 防禦層的穩健性。  
* **執行：**  
  * (此步驟為進階選項，視時間與資源決定是否在第一階段實作)

\#\#\# \*\*檔案說明 (Files Summary)\*\*

本計畫將生成 4 個主要的 Python 腳本對應上述步驟：

1\.  \`01\_data\_and\_features\_v5.py\`: 涵蓋步驟 0 與 1，負責數據下載與特徵工程。  
2\.  \`02\_train\_regime\_model\_l1.py\`: 涵蓋步驟 2，訓練 HMM 與 Isolation Forest。  
3\.  \`03\_train\_meta\_labeling\_l3.py\`: 涵蓋步驟 3，生成基礎訊號並訓練元標籤模型。  
4\.  \`04\_backtest\_and\_verify.py\`: 涵蓋步驟 4，執行驗證協議與產出報告。  
